import type { MariflagConfig } from '../types/config';

export const DEFAULT_CONFIG: Required<MariflagConfig> = {
  orientation: 'horizontal',
  offset: 16, // 1/3 of the flag size
  customIcons: {},
};
